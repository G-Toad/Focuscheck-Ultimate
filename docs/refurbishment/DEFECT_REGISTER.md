# Defect Register

| ID | Severity | Classification | Evidence | Fix | Tests | Manual verification | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CFG-001 | 1 | confirmed defect | Malformed settings silently fell back and destroyed recovery context. | Quarantine corrupt file and recover `.bak`. | `SettingsSaveTests`. | Not run. | fixed |
| CFG-002 | 1 | confirmed defect | Save had no structured observable durability result. | `SettingsSaveResult` exposes path, revision, durable-write, backup, validation, conflict, and error fields while remaining bool-compatible. | settings result and write-failure regressions. | Not run. | fixed |
| CFG-003 | 2 | confirmed defect | Date validation accepted impossible calendar dates. | `datetime.date` validation. | settings validation regression. | Not run. | fixed |
| SUP-003 | 1 | confirmed defect | Supervisor killed every `WerFault.exe` process system-wide. | Compatibility hook is a no-op; no call sites remain. | source review and compile. | Target Windows crash test required. | fixed |
| SUP-001 | 1 | confirmed defect | Normal launcher forced start over durable pause. | Removed hidden force-start precedence from normal launch; explicit `--force-start` is the only override and is forwarded through direct/supervised entrypoints. | launch script, startup-state, and supervisor forwarding regressions. | Live supervised pause test required. | fixed |
| SUP-007 | 1 | confirmed defect | Two heartbeat writers/files existed. | Single App-owned JSON heartbeat. | self-tests and verification runner. | Live hang/restart test required. | fixed |
| SUP-004 | 1 | confirmed defect | Intentional-stop marker was existence/PID-only and could be stale or foreign. | Atomic structured request bound to supervisor, generation, PID, process-start time, nonce, and freshness window. | `SupervisorEntrypointTests`, lifecycle tests. | Live user-exit race required. | fixed |
| SUP-005 | 2 | confirmed defect | PID-only supervisor lock could be ambiguous under PID reuse. | Structured owner record with process-start token and instance nonce; ownership-checked release. | `test_stale_supervisor_lock_recovers_after_pid_reuse`, lock lifecycle tests. | Live duplicate-supervisor test required. | fixed |
| SUP-006 | 2 | confirmed defect | Wall-clock heartbeat age could misclassify clock changes and was shorter than the file heartbeat cadence. | Sequence receipt-time freshness with explicit heartbeat interval and legacy wall-clock fallback. | `test_supervisor_uses_sequence_receipt_time_not_wall_clock_age`, heartbeat protocol tests. | Live sleep/resume and hang test required. | fixed |
| SUP-002 | 1 | confirmed defect | Restart delay reset was not explicitly tied to a stable ready child. | Reset exponential backoff only after a current-generation ready heartbeat remains healthy for `STABLE_RUNTIME_SECONDS`. | `test_restart_backoff_resets_only_after_stable_ready_window`. | Live crash-loop and recovery test required. | fixed |
| SUP-008 | 1 | confirmed defect | Heartbeat write errors were swallowed without consecutive-failure evidence. | Atomic heartbeat publication, consecutive failure counter, and throttled structured diagnostics. | `test_app_heartbeat_write_failure_is_counted_and_throttled`. | Live permission/disk failure test required. | fixed |
| SUP-010 | 1 | confirmed defect | Supervisor stop handling had no durable acknowledgement and restart waits could delay explicit termination. | Cancellation-aware event waits plus atomic generation-bound `supervisor.stop.ack` publication for accepted requests; acknowledgements now report `graceful`, `already_exited`, or `forced_after_timeout` termination outcomes. | `test_intentional_stop_is_acknowledged_for_current_generation`, foreign-generation rejection test, `test_child_termination_timeout_is_reported_and_forced`. | Live user-exit and shutdown-timeout test required. | fixed |
| SNOOZE-001 | 1 | confirmed defect | Direct snooze writes could be overwritten by pause mutation, and expiry could clear manual pause. | Coordinator-owned snooze start/cancel/expiry transitions preserve manual pause and persist independently. | `test_snooze_state_preserves_manual_pause_and_expires_without_clearing_it`. | Live tray snooze matrix required. | fixed |
| SNOOZE-002 | 2 | confirmed defect | Snooze reminders inferred snooze from `paused=True`, including deliberate manual pauses. | Require a valid, unexpired `snooze_until_utc` before scheduling a re-enable reminder. | `test_snooze_reminder_ignores_manual_pause_without_snooze_expiry`. | Live manual-pause versus snooze reminder matrix required. | fixed |
| SNOOZE-003 | 2 | confirmed defect | Snooze start, startup expiry, tray expiry, and cancellation duplicated timer/state logic and could clear manual pause, persist expired snooze as paused, or double-cancel registry timers. | Shared `_expire_snooze` and `_cancel_snooze_timer` paths use the runtime clock/coordinator and named timer owner; persisted pause now uses clock-aware snooze activity; standalone fallback expiry preserves manual pause and resnoozing cancels registry-owned timers even without a legacy callback ID. | `test_expired_startup_snooze_preserves_manual_pause`, `test_expired_startup_snooze_fallback_preserves_manual_pause`, `test_snooze_expiry_callback_preserves_manual_pause`, `test_snooze_expiry_fallback_preserves_manual_pause`, `test_cancel_snooze_timer_cancels_registry_without_legacy_id`, `test_expired_snooze_does_not_persist_as_paused`, injected-clock reconciliation and property tests. | Live sleep/clock-change and tray snooze matrix required. | fixed |
| SNOOZE-004 | 2 | confirmed defect | Startup selected monitoring state before reconciling snooze expiry, so an expired effective `paused=True` could be converted into new durable manual pause even when `manual_paused=False`. | Reconcile startup snooze state before initial monitoring decision and assert expired legacy snooze restarts unpaused without manual intent. | `test_expired_startup_snooze_does_not_create_manual_pause`, corrected `state_restart_selftest`, full verification. | Live restart with expired snooze and manual-pause distinction required. | fixed |
| GUARD-001 | 1 | confirmed defect | Windows idle guard mixed 64-bit uptime with 32-bit last-input ticks without explicit signatures. | Declare API widths and use unsigned 32-bit wrap-safe subtraction. | `test_windows_idle_guard_declares_pointer_width_and_wrap_safe_ticks`. | Live idle/lock/sleep test required. | fixed |
| GUARD-002 | 2 | confirmed defect | Native pause-guard failures degraded silently to no-pause. | Expose bounded guard health/source metadata in heartbeat and diagnostics while retaining fail-open behavior. | `test_windows_idle_guard_exposes_native_api_failure_diagnostics`. | Live native failure/permission test required. | fixed |
| GUARD-003 | 1 | confirmed defect | `App._refresh_guard_state()` recursively called itself and converted the resulting recursion failure into `guard_paused=False`, bypassing lock/idle/sleep pause state. | Sample `PauseGuard.should_pause()` once and publish the result to `RuntimeStateCoordinator`. | `test_refresh_guard_samples_guard_once_and_publishes_state`. | Live lock/idle/sleep matrix required. | fixed |
| START-002 | 2 | confirmed defect | Startup inspection treated any non-empty registry value as installed and ignored moved/legacy/duplicate routes. | Classify absent, valid, stale, malformed, legacy, and duplicate states; expose explicit repair. | startup inspection and repair regressions. | Live registry/moved-install test required. | fixed |
| START-003 | 2 | confirmed defect | Registry Run and Startup-folder launchers could coexist without duplicate detection. | Registry is canonical; legacy launcher is classified and removed only by explicit repair. | legacy/duplicate inspection and repair regressions. | Live duplicate-startup test required. | fixed |
| START-006 | 2 | confirmed defect | Named single-instance mutex handles had no explicit API signatures or lifecycle release. | Declare Win32 signatures and release the owned handle from `main()` in `finally`. | `test_single_instance_mutex_handle_is_released`. | Live duplicate-launch/focus behavior required. | fixed |
| LIFE-001 | 1 | confirmed defect | Windows `query_end_session` committed an immediate `os._exit(0)`, bypassing persistence and committed shutdown handling. | Treat query as preparation; route committed `end_session` through the normal idempotent cleanup coordinator. | Windows shutdown query/end-session lifecycle regressions. | Live Windows logoff/shutdown test required. | fixed |
| UI-THREAD-002 | 1 | confirmed defect | Tray task-dialog command could construct Tk widgets from the pystray thread. | Route task-dialog creation through the recorded Tk owner-thread dispatcher. | `test_tray_task_dialog_is_created_only_on_tk_owner_thread`. | Live tray task-dialog test required. | fixed |
| TRAY-004 | 1 | confirmed defect | Tray exit used `sys.exit` as a fallback from the pystray thread. | Delegate lifecycle termination to the App and stop the icon without tray-thread process exit. | `test_enabled_exit_delegates_without_tray_thread_sys_exit`. | Live tray exit test required. | fixed |
| PROMPT-002 | 1 | confirmed defect | `App.run()` converted mainloop exceptions into exit code `0` in `finally`. | Cleanup now preserves the original exception; explicit app quit remains lifecycle-owned. | `test_run_preserves_mainloop_exception_after_cleanup`. | Live shutdown/error test required. | fixed |
| PROMPT-003 | 2 | confirmed defect | `_maybe_show_prompt()` reparsed settings from disk on every scheduler tick, making prompt eligibility depend on hot-path I/O and partial external writes. | Use the validated in-memory App snapshot; reload only at startup and explicit settings/tray entry points. | `test_prompt_tick_uses_in_memory_settings_snapshot`. | Live external-settings policy test required. | fixed |
| PATH-001 | 1 | confirmed defect | Legacy task/log files could remain outside the canonical data root after settings migration. | Explicit atomic task/log migration with hash-preserved conflicts and metadata-only journal. | `test_legacy_task_and_log_data_migrates_without_destroying_conflicts`. | Existing-user migration test required. | fixed |
| PATH-002 | 1 | confirmed defect | App heartbeat writes could follow an import-time path constant instead of the App-owned frozen data-root snapshot. | Resolve the heartbeat target from `AppPaths` at composition time, retaining a compatibility fallback for lifecycle tests. | `test_app_heartbeat_uses_frozen_app_paths_snapshot`. | Isolated live supervised-root test required. | fixed |
| PATH-003 | 1 | confirmed defect | Application logging independently selected its file path, allowing the first logger handler to diverge from the App composition root. | Bind the logger path during App composition before the first logger use, with a standalone fallback for non-composed callers. | `test_logger_uses_composition_root_path_before_first_use`. | Isolated live log-root test required. | fixed |
| PATH-004 | 1 | confirmed defect | CSV and intervention-reflection writers retained module-import paths instead of the App-owned immutable data-root snapshot. | Rebind all four CSV/JSONL targets during App composition through `configure_paths`. | `test_csv_logger_paths_follow_composed_app_paths`. | Isolated live log-root test required. | fixed |
| DB-003 | 2 | confirmed defect | No database invariant prevented duplicate active tasks. | Unique partial index and legacy reconciliation. | TaskDB lifecycle tests. | Concurrent live UI test required. | fixed |
| LOGDATA-001 | 2 | confirmed defect | CSV rotation happened outside the per-file lock. | Rotation now occurs inside the lock. | CSV tests. | Not run. | fixed |
| LOGDATA-002 | 2 | confirmed defect | Intervention reflection JSONL had no per-record size bound or corruption-tolerant reader. | Reject records over `256 KiB`, retain bounded rotated backups, and expose a reader that skips malformed/oversized lines. | `test_jsonl_reflections_bound_records_rotate_and_skip_corrupt_lines`, rotation regression. | Not run. | fixed |
| WIN-001 | 2 | confirmed defect | Registered overlay classes retained their WNDPROC callback only on an individual instance, allowing callback collection after instance teardown. | Retain each registered callback at module scope and reuse it for subsequent instances. | `test_overlay_reuse_retains_process_global_wndproc`, native overlay self-test. | Target Windows overlay stress required. | fixed |
| INT-003 | 2 | confirmed defect | Spotlight region handles could leak when `SetWindowRgn` failed or region construction raised after partial allocation. | Track window ownership of the combined region and release all temporary regions on every path. | `test_spotlight_region_cleanup_respects_window_ownership`, full verification. | Target Windows intervention stress required. | fixed |
| WIN-004 | 2 | confirmed defect | Click-through and fallback WNDPROC helpers crossed the User32 boundary without shared pointer-sized signatures and used a separate unconfigured fallback handle. | Centralize style/positioning/callback declarations and dispatch fallback calls through the configured User32 handle. | `test_window_style_helpers_declare_shared_native_signatures`, dialog equivalent, full verification. | Target Windows click-through/DPI test required. | fixed |
| WIN-005 | 2 | confirmed defect | Native tray/session watcher calls for message registration, metrics, icon loading, icon cleanup, and fallback dispatch were not centrally typed. | Declare watcher User32 signatures once and route icon cleanup/fallback dispatch through the configured handles. | `test_watcher_declares_tray_and_session_native_signatures`, full verification. | Target Windows tray/session/DPI test required. | fixed |
| WIN-006 | 2 | confirmed defect | Prompt Windows integration crossed style, taskbar, focus, and thread-attach APIs without a shared pointer/thread signature boundary. | Declare User32/Kernel32 integration signatures once and use them for prompt focus, flash, and style operations. | `test_prompt_windows_integration_declares_focus_and_style_signatures`, full verification. | Target Windows prompt focus/DPI test required. | fixed |
| WIN-007 | 2 | confirmed defect | V2 subpopup and phrase-acronym prompt focus paths crossed User32 metrics/foreground/thread APIs without explicit signatures. | Type the subpopup metrics call and reuse the prompt integration boundary for phrase focus operations. | `test_v2_subpopup_declares_virtual_screen_metrics`, full verification. | Target Windows prompt focus/DPI test required. | fixed |
| WIN-008 | 2 | confirmed defect | Prompt monitor enumeration used a floating-point callback data parameter instead of pointer-sized Win32 `LPARAM` and left monitor APIs untyped. | Declare monitor API signatures and use `LPARAM_T` in the enumeration callback. | `test_window_placement_declares_pointer_safe_monitor_signatures`, full verification. | Target Windows multi-monitor/DPI test required. | fixed |
| WIN-009 | 2 | confirmed defect | Intensification gamma and Magnification paths crossed device-context, gamma-ramp, and fullscreen color-effect APIs without explicit signatures. | Declare the gamma and Magnification lifecycle APIs before prepare/apply/restore operations. | `test_intensification_declares_gamma_and_magnification_signatures`, full verification. | Target Windows gamma/Magnification hardware test required. | fixed |
| FLAG-007 | 2 | confirmed defect | Browser CDP/UIA results had no target-count or title/URL size bound before matching and return. | Bound CDP targets and UI Automation items, and truncate returned tab text/URLs at provider boundaries. | `test_discovery_bounds_targets_and_text_fields`, `test_list_tab_titles_bounds_and_deduplicates`, full verification. | Live supported-browser provider matrix required. | fixed |
| VERIFY-004 | 1 | confirmed defect | Verification timeout handling reported a timeout but left descendant processes uncontrolled. | Launch each stage in its own process group and terminate only that stage PID tree, with a fallback process kill and recorded cleanup metadata. | `test_timeout_is_reported_as_a_distinct_stage_status`, `test_timeout_cleanup_does_not_target_processes_by_image_name`, full verification. | Target Windows timeout/child-tree test required. | fixed |
| VERIFY-005 | 2 | confirmed defect | Verification did not explicitly inventory automated, live-Windows, manual-only, and destructive test categories. | Checked-in category manifest and bounded inventory stage classify coverage without treating manual categories as automated passes. | `test_test_category_manifest_covers_automated_and_manual_classes`, `test_category_inventory`, full verification. | Manual category execution remains required. | fixed |
| PROMPT-004 | 1 | confirmed defect | V1 `_finish()` and V2 `_close()` allowed timer-cleanup exceptions to skip dialog destruction and owner notification. | Treat timer cleanup as one failure-isolated finalization step, then always destroy the dialog and notify the owner exactly once. | `test_v1_prompt_finishes_when_timer_cleanup_raises`, `test_v2_prompt_closes_when_timer_cleanup_raises`, full verification. | Live prompt interruption/close matrix required. | fixed |
| PROMPT-005 | 1 | confirmed defect | Focus/waste follow-up and acronym dialogs invoked their completion callbacks only after an unguarded `destroy()`, so teardown errors could leave the parent prompt latched. | Isolate child-dialog destruction failures and always invoke the intended cancel/submit/complete callback. | `test_follow_up_dialog_callbacks_survive_destroy_failures`, full verification. | Live prompt interruption/close matrix required. | fixed |
| PAUSE-001 | 1 | confirmed defect | Durable `paused` conflated manual intent with snooze-owned effective pause, making restart/expiry recovery ambiguous. | Add state-only `manual_paused`, migrate legacy documents, derive compatibility `paused`, and preserve raw-dictionary behavior for non-repository callers. | `test_legacy_paused_snooze_is_not_reclassified_as_manual_intent`, `test_explicit_manual_intent_survives_snooze_expiry`, settings migration tests, full verification. | Live tray/restart snooze matrix required. | fixed |
| DB-009 | 2 | confirmed defect | Task UI due-date branches swallowed `NameError` because the mixin omitted datetime imports, allowing overdue tasks to be marked completed; DB overdue processing also omitted timeout attribution. | Import the time types used by the task UI and persist overdue transitions as `timed_out=1` with an auditable reason. | `test_prompt_task_done_marks_overdue_task_as_timed_out`, overdue TaskDB regression, full verification. | Live task UI deadline flow required. | fixed |
| ACTIVITY-001 | 2 | confirmed defect | Provider mappings could include an older `captured_utc`, but `ActivitySnapshot.from_mapping()` discarded it and stamped the snapshot with the current time, making stale activity appear fresh to Engine V2. | Preserve and validate provider capture timestamps; stale or errored snapshots remain unusable for website intervention. | `test_provider_capture_timestamp_is_preserved_and_invalid_values_fallback`, `test_stale_activity_is_not_usable_for_website_intervention`, full verification. | Live supported-browser/provider freshness matrix required. | fixed |
| UI-019 | 2 | confirmed defect | V2 website sub-popup acquired a Tk grab but relied on destruction implicitly releasing it, leaving grab ownership unexplicit on close paths. | Release the Tk grab from the idempotent `destroy()` path before superclass destruction. | `test_v2_subpopup_escape_declines`, full verification. | Live V2 Escape/close and focus-restoration test required. | fixed |
| SUP-011 | 2 | confirmed defect | App stop requests and supervisor stop acknowledgements performed durable writes but returned no result, so shutdown callers could not distinguish an acknowledged request from a write failure. | Return explicit boolean durability results and log an App-side warning when the stop request is not confirmed. | `test_app_quit_writes_supervisor_stop_request`, `test_app_supervisor_stop_request_without_supervisor_is_explicit_failure`, acknowledgement success/failure regressions, full verification. | Live user-exit acknowledgement and shutdown-timeout test required. | fixed |
| INT-004 | 2 | confirmed defect | `InterventionWizard.run()` marshalled off-thread execution with a direct `parent.after()` callback outside its existing timer owner, so timeout cleanup could not cancel the scheduled callback. | Route dispatch through the wizard `TimerRegistry` and cancel the named dispatch entry when the bounded wait expires. | `test_off_thread_intervention_timeout_invalidates_queued_tk_callback`, full verification. | Live intervention timeout and shutdown test required. | fixed |
| INT-005 | 2 | confirmed defect | Intervention selection failure and visibility fail-safe paths restored a hidden prompt with direct `prompt_ref.after()` callbacks, outside the prompt's timer owner. | Route delayed focus recovery through the prompt `TimerRegistry`, falling back to the wizard registry only for standalone fixtures. | `test_prompt_focus_recovery_uses_prompt_timer_owner`, full verification. | Live prompt restoration and focus test required. | fixed |
| PROMPT-006 | 2 | confirmed defect | Prompt Windows integration restored an iconic prompt with direct `self.after()` scheduling, bypassing the prompt timer registry and its cleanup contract. | Route minimize recovery through `_schedule_timer()` when available, retaining a standalone compatibility fallback. | `test_prompt_minimize_recovery_uses_prompt_timer_owner`, full verification. | Live minimize/focus restoration test required. | fixed |
| PROMPT-007 | 2 | confirmed defect | Focus and Waste detail dialogs scheduled initial focus with `after_idle()` but had no owned timer registry, allowing a queued callback to target a destroyed dialog. | Give both dialogs a `TimerRegistry`, schedule initial focus through it, and close the registry from `destroy()`. | `test_focus_and_waste_dialogs_own_initial_focus_timer`, full verification. | Live detail-dialog close/focus test required. | fixed |
| PROMPT-008 | 2 | confirmed defect | App shutdown closed the gentle reminder but omitted an open snooze reminder from the explicit dialog cleanup sequence. | Add an App-owned snooze-reminder close path that does not emit a synthetic user callback; make direct dialog destruction release its focus timer registry. | `test_shutdown_closes_snooze_reminder_before_root_destroy`, snooze reminder keyboard/resource tests, full verification. | Live shutdown with an open snooze reminder required. | fixed |
| PROMPT-009 | 2 | confirmed defect | App shutdown also omitted the tray snooze confirmation dialog, leaving its grab and owned focus/visibility timers outside the explicit cleanup sequence. | Add an App-owned callback-free close path and make direct confirmation-dialog destruction cancel its timer registry. | shutdown cleanup-order regression, snooze-prompt timer cleanup tests, full verification. | Live shutdown while snooze confirmation is open required. | fixed |
| UI-020 | 2 | confirmed defect | The App-owned diagnostic status window was not included in the explicit shutdown resource sequence and relied on root destruction. | Close and clear the status-window reference before engine/root teardown. | shutdown cleanup-order regression, status-window lifecycle tests, full verification. | Live shutdown with the status window open required. | fixed |
| PROMPT-010 | 2 | confirmed defect | V1 Focus/Waste detail dialogs were not retained by their parent prompt, so interruption cleanup could not explicitly release their grab or timer registry and could leave `_focus_prompt_open` latched. | Retain one parent-owned follow-up dialog, add callback-free interruption close methods, and close/reset it before parent timer/destruction cleanup. | `test_v1_detail_dialogs_close_with_parent_interruption`, valid detail-flow regression, full verification. | Live pause/shutdown while a detail dialog is open required. | fixed |
| PROMPT-011 | 2 | confirmed defect | The V1 phrase-acronym challenge was also created without parent ownership, leaving its grab and timers outside prompt interruption cleanup. | Retain the acronym dialog under the same follow-up owner, separate user cancellation from callback-free interruption, and clear ownership on synchronous completion. | `test_v1_acronym_dialog_closes_with_parent_interruption`, `test_phrase_challenge_closes_owned_timers`, full verification. | Live pause/shutdown while acronym challenge is open required. | fixed |
| SCHED-002 | 2 | confirmed defect | Gentle-reminder eligibility read raw `settings["paused"]` instead of coordinator-owned effective pause, allowing reminders during guard pause or other non-persisted pause reasons. | Route eligibility through `RuntimeStateCoordinator.is_effectively_paused()` with standalone compatibility fallback. | `test_gentle_reminder_uses_effective_runtime_pause`, full verification. | Live guard/lock/sleep gentle-reminder matrix required. | fixed |
| TRAY-006 | 2 | confirmed defect | Tray Pause/Resume selection read compatibility `settings["paused"]`, treating snooze or guard pause as manual pause intent. | Select Resume only from coordinator-owned `snapshot.manual_paused`, with standalone compatibility fallback. | `test_tray_toggle_uses_manual_pause_not_effective_guard_pause`, full verification. | Live tray/manual-pause versus guard/snooze matrix required. | fixed |
| TRAY-007 | 2 | confirmed defect | Fallback `SystemTray` menu state also read compatibility `settings["paused"]`, so its enabled actions could disagree with coordinator-owned manual intent. | Read coordinator `snapshot.manual_paused`, then migrated `manual_paused`, with legacy settings fallback. | `test_fallback_tray_pause_state_uses_manual_runtime_intent`, `test_fallback_tray_pause_state_uses_migrated_manual_setting`, full verification. | Live fallback-tray guard/snooze matrix required. | fixed |
| TRAY-008 | 2 | confirmed defect | App-owned native tray menu state read compatibility `settings["paused"]` directly, so its enabled Pause/Resume actions could disagree with coordinator-owned manual intent. | Centralize manual-pause intent lookup and use it for both App tray menu presentation and command toggling, with standalone compatibility fallback. | `test_manual_pause_intent_uses_coordinator_before_compatibility_settings`, `test_manual_pause_intent_falls_back_to_compatibility_settings`, full verification. | Live native tray guard/snooze matrix required. | fixed |
| OBS-003 | 2 | confirmed defect | Privacy-safe diagnostic status reported raw compatibility `settings["paused"]`, disagreeing with coordinator effective pause during snooze or guard pause. | Report coordinator-derived effective pause in the status snapshot, with standalone compatibility fallback. | `test_diagnostic_status_uses_effective_runtime_pause`, full verification. | Live status-window snooze/guard display required. | fixed |
