# Defect Register

| ID | Severity | Classification | Evidence | Fix | Tests | Manual verification | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CFG-001 | 1 | confirmed defect | Malformed settings silently fell back and destroyed recovery context. | Quarantine corrupt file and recover `.bak`. | `SettingsSaveTests`. | Not run. | fixed |
| CFG-002 | 1 | confirmed defect | Save had no structured observable durability result. | `SettingsSaveResult` exposes path, revision, durable-write, backup, validation, conflict, and error fields while remaining bool-compatible. | settings result and write-failure regressions. | Not run. | fixed |
| CFG-003 | 2 | confirmed defect | Date validation accepted impossible calendar dates. | `datetime.date` validation. | settings validation regression. | Not run. | fixed |
| SUP-003 | 1 | confirmed defect | Supervisor killed every `WerFault.exe` process system-wide. | Compatibility hook is a no-op; no call sites remain. | source review and compile. | Target Windows crash test required. | fixed |
| SUP-001 | 1 | confirmed defect | Normal launcher forced start over durable pause. | Removed force-start from normal launcher. | launch script regression. | Live supervised pause test required. | fixed |
| SUP-007 | 1 | confirmed defect | Two heartbeat writers/files existed. | Single App-owned JSON heartbeat. | self-tests and verification runner. | Live hang/restart test required. | fixed |
| SUP-004 | 1 | confirmed defect | Intentional-stop marker was existence/PID-only and could be stale or foreign. | Atomic structured request bound to supervisor, generation, PID, process-start time, nonce, and freshness window. | `SupervisorEntrypointTests`, lifecycle tests. | Live user-exit race required. | fixed |
| SUP-005 | 2 | confirmed defect | PID-only supervisor lock could be ambiguous under PID reuse. | Structured owner record with process-start token and instance nonce; ownership-checked release. | `test_stale_supervisor_lock_recovers_after_pid_reuse`, lock lifecycle tests. | Live duplicate-supervisor test required. | fixed |
| SUP-006 | 2 | confirmed defect | Wall-clock heartbeat age could misclassify clock changes and was shorter than the file heartbeat cadence. | Sequence receipt-time freshness with explicit heartbeat interval and legacy wall-clock fallback. | `test_supervisor_uses_sequence_receipt_time_not_wall_clock_age`, heartbeat protocol tests. | Live sleep/resume and hang test required. | fixed |
| SUP-002 | 1 | confirmed defect | Restart delay reset was not explicitly tied to a stable ready child. | Reset exponential backoff only after a current-generation ready heartbeat remains healthy for `STABLE_RUNTIME_SECONDS`. | `test_restart_backoff_resets_only_after_stable_ready_window`. | Live crash-loop and recovery test required. | fixed |
| SUP-008 | 1 | confirmed defect | Heartbeat write errors were swallowed without consecutive-failure evidence. | Atomic heartbeat publication, consecutive failure counter, and throttled structured diagnostics. | `test_app_heartbeat_write_failure_is_counted_and_throttled`. | Live permission/disk failure test required. | fixed |
| SUP-010 | 1 | confirmed defect | Supervisor stop handling had no durable acknowledgement and restart waits could delay explicit termination. | Cancellation-aware event waits plus atomic generation-bound `supervisor.stop.ack` publication for accepted requests. | `test_intentional_stop_is_acknowledged_for_current_generation`, foreign-generation rejection test. | Live user-exit and shutdown-timeout test required. | fixed |
| SNOOZE-001 | 1 | confirmed defect | Direct snooze writes could be overwritten by pause mutation, and expiry could clear manual pause. | Coordinator-owned snooze start/cancel/expiry transitions preserve manual pause and persist independently. | `test_snooze_state_preserves_manual_pause_and_expires_without_clearing_it`. | Live tray snooze matrix required. | fixed |
| SNOOZE-002 | 2 | confirmed defect | Snooze reminders inferred snooze from `paused=True`, including deliberate manual pauses. | Require a valid, unexpired `snooze_until_utc` before scheduling a re-enable reminder. | `test_snooze_reminder_ignores_manual_pause_without_snooze_expiry`. | Live manual-pause versus snooze reminder matrix required. | fixed |
| SNOOZE-003 | 2 | confirmed defect | Snooze start, startup expiry, tray expiry, and cancellation duplicated timer/state logic and could clear manual pause, persist expired snooze as paused, or double-cancel registry timers. | Shared `_expire_snooze` and `_cancel_snooze_timer` paths use the runtime clock/coordinator and named timer owner; persisted pause now uses clock-aware snooze activity. | `test_expired_startup_snooze_preserves_manual_pause`, `test_snooze_expiry_callback_preserves_manual_pause`, `test_expired_snooze_does_not_persist_as_paused`, injected-clock reconciliation and property tests. | Live sleep/clock-change and tray snooze matrix required. | partial |
| GUARD-001 | 1 | confirmed defect | Windows idle guard mixed 64-bit uptime with 32-bit last-input ticks without explicit signatures. | Declare API widths and use unsigned 32-bit wrap-safe subtraction. | `test_windows_idle_guard_declares_pointer_width_and_wrap_safe_ticks`. | Live idle/lock/sleep test required. | fixed |
| GUARD-002 | 2 | confirmed defect | Native pause-guard failures degraded silently to no-pause. | Expose bounded guard health/source metadata in heartbeat and diagnostics while retaining fail-open behavior. | `test_windows_idle_guard_exposes_native_api_failure_diagnostics`. | Live native failure/permission test required. | fixed |
| GUARD-003 | 1 | confirmed defect | `App._refresh_guard_state()` recursively called itself and converted the resulting recursion failure into `guard_paused=False`, bypassing lock/idle/sleep pause state. | Sample `PauseGuard.should_pause()` once and publish the result to `RuntimeStateCoordinator`. | `test_refresh_guard_samples_guard_once_and_publishes_state`. | Live lock/idle/sleep matrix required. | fixed |
| START-002 | 2 | confirmed defect | Startup inspection treated any non-empty registry value as installed and ignored moved/legacy/duplicate routes. | Classify absent, valid, stale, malformed, legacy, and duplicate states; expose explicit repair. | startup inspection and repair regressions. | Live registry/moved-install test required. | fixed |
| START-003 | 2 | confirmed defect | Registry Run and Startup-folder launchers could coexist without duplicate detection. | Registry is canonical; legacy launcher is classified and removed only by explicit repair. | legacy/duplicate inspection and repair regressions. | Live duplicate-startup test required. | fixed |
| START-006 | 2 | confirmed defect | Named single-instance mutex handles had no explicit API signatures or lifecycle release. | Declare Win32 signatures and release the owned handle from `main()` in `finally`. | `test_single_instance_mutex_handle_is_released`. | Live duplicate-launch/focus behavior required. | fixed |
| LIFE-001 | 1 | confirmed defect | Windows `query_end_session` committed an immediate `os._exit(0)`, bypassing persistence and committed shutdown handling. | Treat query as preparation; route committed `end_session` through the normal idempotent cleanup coordinator. | Windows shutdown query/end-session lifecycle regressions. | Live Windows logoff/shutdown test required. | fixed |
| UI-THREAD-002 | 1 | confirmed defect | Tray task-dialog command could construct Tk widgets from the pystray thread. | Route task-dialog creation through the recorded Tk owner-thread dispatcher. | `test_tray_task_dialog_is_created_only_on_tk_owner_thread`. | Live tray task-dialog test required. | fixed |
| TRAY-004 | 1 | confirmed defect | Tray exit used `sys.exit` as a fallback from the pystray thread. | Delegate lifecycle termination to the App and stop the icon without tray-thread process exit. | `test_enabled_exit_delegates_without_tray_thread_sys_exit`. | Live tray exit test required. | fixed |
| PROMPT-002 | 1 | confirmed defect | `App.run()` converted mainloop exceptions into exit code `0` in `finally`. | Cleanup now preserves the original exception; explicit app quit remains lifecycle-owned. | `test_run_preserves_mainloop_exception_after_cleanup`. | Live shutdown/error test required. | fixed |
| PROMPT-003 | 2 | confirmed defect | `_maybe_show_prompt()` reparsed settings from disk on every scheduler tick, making prompt eligibility depend on hot-path I/O and partial external writes. | Use the validated in-memory App snapshot; reload only at startup and explicit settings/tray entry points. | `test_prompt_tick_uses_in_memory_settings_snapshot`. | Live external-settings policy test required. | fixed |
| PATH-001 | 1 | confirmed defect | Legacy task/log files could remain outside the canonical data root after settings migration. | Explicit atomic task/log migration with hash-preserved conflicts and metadata-only journal. | `test_legacy_task_and_log_data_migrates_without_destroying_conflicts`. | Existing-user migration test required. | fixed |
| PATH-002 | 1 | confirmed defect | App heartbeat writes could follow an import-time path constant instead of the App-owned frozen data-root snapshot. | Resolve the heartbeat target from `AppPaths` at composition time, retaining a compatibility fallback for lifecycle tests. | `test_app_heartbeat_uses_frozen_app_paths_snapshot`. | Isolated live supervised-root test required. | fixed |
| PATH-003 | 1 | confirmed defect | Application logging independently selected its file path, allowing the first logger handler to diverge from the App composition root. | Bind the logger path during App composition before the first logger use, with a standalone fallback for non-composed callers. | `test_logger_uses_composition_root_path_before_first_use`. | Isolated live log-root test required. | fixed |
| DB-003 | 2 | confirmed defect | No database invariant prevented duplicate active tasks. | Unique partial index and legacy reconciliation. | TaskDB lifecycle tests. | Concurrent live UI test required. | fixed |
| LOGDATA-001 | 2 | confirmed defect | CSV rotation happened outside the per-file lock. | Rotation now occurs inside the lock. | CSV tests. | Not run. | fixed |
| LOGDATA-002 | 2 | confirmed defect | Intervention reflection JSONL had no per-record size bound or corruption-tolerant reader. | Reject records over `256 KiB`, retain bounded rotated backups, and expose a reader that skips malformed/oversized lines. | `test_jsonl_reflections_bound_records_rotate_and_skip_corrupt_lines`, rotation regression. | Not run. | fixed |
| WIN-001 | 2 | confirmed defect | Registered overlay classes retained their WNDPROC callback only on an individual instance, allowing callback collection after instance teardown. | Retain each registered callback at module scope and reuse it for subsequent instances. | `test_overlay_reuse_retains_process_global_wndproc`, native overlay self-test. | Target Windows overlay stress required. | fixed |
| INT-003 | 2 | confirmed defect | Spotlight region handles could leak when `SetWindowRgn` failed or region construction raised after partial allocation. | Track window ownership of the combined region and release all temporary regions on every path. | `test_spotlight_region_cleanup_respects_window_ownership`, full verification. | Target Windows intervention stress required. | fixed |
| WIN-004 | 2 | confirmed defect | Click-through and fallback WNDPROC helpers crossed the User32 boundary without shared pointer-sized signatures and used a separate unconfigured fallback handle. | Centralize style/positioning/callback declarations and dispatch fallback calls through the configured User32 handle. | `test_window_style_helpers_declare_shared_native_signatures`, dialog equivalent, full verification. | Target Windows click-through/DPI test required. | fixed |
| WIN-005 | 2 | confirmed defect | Native tray/session watcher calls for message registration, metrics, icon loading, icon cleanup, and fallback dispatch were not centrally typed. | Declare watcher User32 signatures once and route icon cleanup/fallback dispatch through the configured handles. | `test_watcher_declares_tray_and_session_native_signatures`, full verification. | Target Windows tray/session/DPI test required. | fixed |
| WIN-006 | 2 | confirmed defect | Prompt Windows integration crossed style, taskbar, focus, and thread-attach APIs without a shared pointer/thread signature boundary. | Declare User32/Kernel32 integration signatures once and use them for prompt focus, flash, and style operations. | `test_prompt_windows_integration_declares_focus_and_style_signatures`, full verification. | Target Windows prompt focus/DPI test required. | fixed |
| WIN-007 | 2 | confirmed defect | V2 subpopup and phrase-acronym prompt focus paths crossed User32 metrics/foreground/thread APIs without explicit signatures. | Type the subpopup metrics call and reuse the prompt integration boundary for phrase focus operations. | `test_v2_subpopup_declares_virtual_screen_metrics`, full verification. | Target Windows prompt focus/DPI test required. | fixed |
