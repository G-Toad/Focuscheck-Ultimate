# Refurbishment Plan Audit

Audit basis: `focuscheck_ultimate_repo_grounded_refurbishment_plan_v1.md`, inspected against repository state at the current refurbishment checkpoint.

Status meanings:

- **Implemented**: the plan requirement has a direct implementation and matching evidence.
- **Partial**: some requested work exists, but one or more explicit requirements are missing.
- **Missing**: no credible implementation or evidence was found.
- **Unverified**: implementation may exist, but the plan requires runtime/manual evidence that is absent.

## Phase Audit

| Plan phase | Status | Evidence and gap |
| --- | --- | --- |
| 0 Repository truth and baseline | Partial | Baseline tests, compile, self-tests, and inventory were captured. Behavior snapshots, profile/registry non-mutation proof, and manual release baseline are absent. |
| 1 Safety and test isolation | Partial | WerFault broad kill was removed, verification uses a disposable data root, hashes the live profile before/after stages, and now guards the repository against unexpected writes plus surviving FocusCheck-owned processes. Settings writes now use an OS-level sidecar lock. Direct `unittest -s tests` still produced live-profile logger contention, and no general filesystem sandbox exists outside the bounded runner. |
| 2 Product contract and contradictions | Partial | Registers and several regression tests exist. The full contradiction/contract closure and complete transition truth tables do not. |
| 3 Verification refurbishment | Partial | Bounded stages, JSON reporting, timeouts, live-profile isolation assertion, repository/process guards, disposable export simulation, and an injectable service clock exist. Comprehensive fault-injection harnesses, thread/window leak checks, and all required test categories do not. |
| 4 Unified paths and data location | Partial | Frozen `AppPaths` covers canonical data/runtime paths, `FOCUS_DATA_DIR` precedence is fixed, package-directory files no longer alter runtime selection, valid legacy settings import atomically, and canonical/legacy conflicts for settings, task DB, and event logs are preserved by hash. Broader migration fixtures and target-user migration evidence remain absent. |
| 5 Settings repository and schema | Partial | V1/V2 migration, quarantine, rotating `.bak/.bak.1/.bak.2` recovery, migration journal, atomic readback-validated save, structured durability results, OS-level cross-process locking, recursive/file-size input budgets, typed schema descriptors, revision/conflict handling, legacy import/conflict fixtures, static UI save-key/schema drift checks, and a runtime-consumer proof for all 170 visible save keys now exist. Complete UI schema generation and broader migration fixtures remain incomplete. |
| 6 Runtime state coordinator | Partial | `RuntimeStateCoordinator` owns transactional pause/snooze mutation, manual-pause-preserving snooze expiry, refreshed settings adoption, exclusive prompt/intervention/shutdown leases, clock-injected prompt eligibility, prompt denial during effective pause, centralized guard refresh/effective-pause decisions, website-check suppression through the coordinator, and an App-wired metadata-only transition journal. Guard health now publishes bounded native failure metadata, but crash/restart recovery evidence remains absent. |
| 7 Scheduler and timer ownership | Partial | Generation-aware `TimerRegistry` now owns App prompt, heartbeat, snooze, and EngineV2 timers, with 1,000-cycle stress coverage. Runtime-state decisions use an injected clock, while Tk timer scheduling remains UI-controlled, dialog/remaining callback timers remain distributed, and the broader stress matrix is absent. |
| 8 Supervisor and heartbeat | Partial | Versioned generation/readiness/sequence heartbeat validation, sequence receipt-time freshness with explicit cadence, stable-ready backoff reset, atomic heartbeat publication with throttled write-failure diagnostics, PID-bound JSON stop requests, nonce/supervisor/generation/process-start/freshness validation, atomic stop writes, durable atomic stop acknowledgements, cancellation-aware restart waits, frozen inner-PID handling, process-start-token lock ownership, stale checks, force-start correction, circuit breaker, and lifecycle tests now exist. Acknowledgement timeout, sleep-gap handling, and the complete failure matrix are not proven. |
| 9 Startup and single-instance | Partial | Registry startup is now the canonical route; non-mutating inspection classifies valid, stale, malformed, legacy, and duplicate registry/folder states, and explicit repair promotes the registry route while removing the known legacy launcher. Frozen packaged startup resolves the sibling supervisor executable with `--run --base-dir`; single-instance mutex API signatures and owned-handle release are explicit. Duplicate process-state correctness and manual registry/moved-install evidence remain absent. |
| 10 Application lifecycle/composition | Partial | `LifecycleCoordinator` now validates constructing/starting/ready/stopping/stopped/failed transitions, App publishes phase metadata in heartbeat, Windows query versus committed end-session handling is separated, shutdown/mainloop failure transitions are covered, App-owned services now share one frozen `AppPaths` snapshot, and shutdown explicitly owns prompt and monitoring-engine cleanup. Full composition-root extraction and failure-injection coverage at every startup/shutdown stage remain open. |
| 11 Tray adapters | Partial | Command tests and self-test exist; all tray actions including task-dialog creation now dispatch through the recorded Tk owner thread, tray exit no longer calls `sys.exit` from the tray thread, and direct JSON persistence fallback was removed. Complete backend state machine and required native manual matrix are not proven. |
| 12 Windows native wrappers | Partial | Native wrappers retain pointer-sized declarations for key window procedures, activity and top-level window probes now declare their Win32 user32/process/PSAPI handle, buffer, pointer, and callback signatures, overlay destruction now releases brush/window handles exactly once under a fake-DLL regression test, and the isolated native overlay self-test passes virtual-screen region updates. Complete Win32 signature audit, native stress harness, and the broader live evidence matrix remain absent. |
| 13 Prompt coordinator | Partial | `PromptCoordinator` now owns one active prompt generation and makes polling, tray close, and destruction completion idempotent. Dialog-specific timer/grab cleanup and the complete close-path matrix remain unverified. |
| 14 V1 prompt flow | Unverified | Existing source and tests cover selected flows; the plan's full visible UI, interruption, persistence, cleanup, and manual evidence matrix was not executed. |
| 15 V2 prompt/activity model | Partial | V2 intervention outcome behavior has regression coverage. Full typed activity snapshots, provider isolation, stale/error behavior, and every intervention outcome are not proven. |
| 16 Website flags | Partial | Canonical hostname normalization now rejects ambiguous URL/port/wildcard input, preserves IDN/IP matching, prevents title fallback from overriding an authoritative non-matching host, defines durable `allow_once` dismissal consumption before cooldown, uses an injected cooldown clock, and blocks severity-3 intervention from medium/low-confidence title-only activity; matching/cooldown tests exist. All paused/locked suppression states, provider matrix, and live browser coverage are absent. |
| 17 Intervention coordinator/overlays | Partial | Cancellation no longer consumes cooldown and App now owns an intervention lease/coordinator entry point. Generation cancellation proof, full cleanup matrix, and overlay manual matrix remain absent. |
| 18 Settings UI | Partial | Existing UI tests, structured save-result/error handling, revision conflict result handling, owner-thread/tray contract tests, static save-key/schema coverage, and runtime-consumer proof for all 170 visible save keys exist. Schema-generated controls, complete visible-control round trips, and manual UI evidence are absent. |
| 19 Task database | Partial | Stronger SQLite setup, active-task index, transition returns, versioned migration journal through schema version 3, integrity-checked numbered pre-migration backups plus checkpointed backup/restore, corrupt-file rejection, concurrent-writer tests, UTC normalization/rejection boundaries, legacy SQL fixture/row normalization with invalid-due repair reasons, and explicit timezone/injected-clock analytics with a DST fixture now exist. UI flow tests and the full migration/timezone matrix remain incomplete. |
| 20 Logs/export/privacy/retention | Partial | CSV locking, JSONL rotation, busy-file-safe application-log rotation, spreadsheet-formula neutralization, diagnostic redaction of user responses, metadata-only data inventory, an atomic allowlisted export with explicit sensitive-category opt-in and SHA-256 manifest, tray/App export, clear-data, and diagnostic-preview controls, and a packaged retention service with symlink rejection, deterministic planning, and metadata-only deletion audits now exist. Export privacy evidence and complete data-inventory coverage remain incomplete. |
| 21 Camera/optional dependencies | Partial | Missing OpenCV/Pillow paths degrade without crashing, capture is opt-in, camera photos use canonical app data or controlled temp recovery rather than the working directory, and failed encodes are reported. Live camera validation and full frame-lifetime evidence remain absent. |
| 22 Browser/activity providers | Partial | Typed activity snapshots normalize provider errors, freshness/confidence, and strip URL query/fragment data; title-only activity is medium confidence and cannot trigger severity-3 intervention; CDP discovery now has a bounded overall deadline and per-request cap with fake-provider tests. Broader provider matrices and live supported-browser evidence remain incomplete. |
| 23 Observability/supportability | Partial | A sanitized bundle generator, bounded Doctor anomaly storage, explicit lifecycle phases, and a bounded privacy-safe structured event ledger now exist; bundles can include structured metadata without user content. Event coverage, throttling policy, user-facing diagnostics, and verified bundle contents across real failures remain incomplete. |
| 24 Security/abuse review | Partial | A dedicated source-level security/abuse review now records data, export, startup, supervisor, and residual-risk controls; penetration testing and target-machine evidence remain absent. |
| 25 Dependencies/packaging | Partial | Runtime requirements are pinned, a dual-executable PyInstaller build and bounded packaged supervisor readiness/reaping self-test pass, and scripted non-destructive install/upgrade/uninstall plus promotion/rollback execute in disposable tests while preserving the data root. Real installer shell integration, signing, long-running packaged supervisor, and target-machine lifecycle evidence remain open. |
| 26 Performance/resource stability | Partial | Explicit core-service budgets and a disposable timer/state/SQLite soak gate now pass. Long-duration UI/native/camera/browser measurements and production-session budgets remain open. |
| 27 Automated test expansion | Partial | 200 unittest cases, self-tests, an isolated native overlay self-test, a bounded packaged supervisor readiness/reaping self-test, a bounded performance soak, repository/process guards, disposable export simulation, frozen-entrypoint/startup and disposable install/upgrade/uninstall/package lifecycle tests, privacy/logging, structured settings-save results, settings/path-migration including task/log conflict migration, runtime-state, centralized guard refresh and duplicate-transition suppression, lifecycle phase transitions, Windows shutdown query/end-session separation, structured event redaction/rotation, retention clock/symlink/audit coverage, settings UI/schema drift and visible runtime-consumer proof, mainloop exception preservation, manual-pause-preserving snooze, tray task-dialog owner-thread dispatch, tray exit lifecycle, tray/App export owner-thread dispatch, metadata-only inventory/clear controls, packaged retention dispatch, diagnostic preview/sanitization, frozen App path-snapshot coverage, idempotent prompt/engine shutdown ownership, activity-confidence, website-domain coercion, heartbeat write-failure, PID-reuse lock, sequence receipt-time freshness, stable-ready backoff, cancellation-aware supervisor waits, durable generation-bound stop acknowledgement, pause-guard native failure diagnostics, startup legacy/duplicate inspection and repair, explicit single-instance mutex handle ownership, activity and window-enumeration ctypes signatures, timezone/DST analytics boundaries, legacy task migration fixture/normalization, integrity-checked migration backup/corruption rejection, allowlisted export manifest/sensitive opt-in, wrap-safe Windows idle-guard matrices, and a native cleanup regression exist. Property tests, broader integration breadth, withdrawn-root Tk tests, native live tests beyond the overlay smoke path, and mutation testing are absent. |
| 28 Manual Windows matrix | Unverified | `docs/refurbishment/manual-evidence.json` explicitly records all five groups as `not_run`. |
| 29 Cleanup after correctness | Missing | The plan's final cleanup/reverification gate cannot be satisfied while release and manual gates remain open. |

## Mandatory Artifacts

All named documentation/report files exist, and migration fixtures plus a diagnostic bundle generator exist. File presence is not completion evidence: `manual-evidence.json` is explicitly `not_run`, and the defect register covers only a subset of the plan's defect register with incomplete required fields such as reproduction, commit/PR, and per-defect manual verification.

The plan's final-release deliverables were also checked individually:

| Required deliverable | Status |
| --- | --- |
| Updated architecture and behaviour specs | Partial; refurbishment maps exist, but the required coordinator architecture is not implemented. |
| Current operation matrix | Present in `docs/refurbishment/CURRENT_OPERATION_MATRIX.md`; manual rows remain explicitly pending. |
| Settings schema/truth table generated or verified | Partial; typed schema descriptors and revision tests exist, but the UI is not generated from the schema and the full truth table is incomplete. |
| Lifecycle/state-machine documentation | Present; implementation and full evidence remain partial. |
| Defect/contradiction registers with no untriaged severity 0/1 | Not met; the defect register is incomplete and several severity-1 items remain unverified. |
| Deterministic verification report | Partial; report is machine-readable and includes a bounded resource-stability gate, but deterministic clock and process/thread/window leak assertions are absent. |
| Manual evidence bundle | Present only as a not-run template. |
| Migration fixtures | Partial; legacy settings import/conflict fixtures exist, but the full required settings/task/data fixture set is absent. |
| Release notes | Present in `docs/RELEASE_NOTES.md`; release status remains `NOT_READY`. |
| Rollback instructions | Present in `docs/ROLLBACK.md` and exercised by the disposable package transaction test; target-machine evidence remains open. |
| Packaging/install instructions | Present with dual-executable PyInstaller, promotion/rollback, and disposable install/upgrade/uninstall contracts; installer shell integration and target-machine evidence remain open. |
| Privacy/data-retention documentation | Partial; privacy notes exist, but retention/data inventory controls are incomplete. |
| Support diagnostic instructions | Present in `docs/SUPPORT_DIAGNOSTICS.md`; redaction is pattern-based and target-machine support evidence remains open. |

## Automated Evidence

Latest verification rerun: `200` unittest cases pass, including centralized guard refresh/duplicate-transition suppression, lifecycle phase transition/failure, Windows shutdown query/end-session separation, structured event redaction/rotation, retention clock/symlink/audit coverage, settings UI/schema drift and visible runtime-consumer proof, cancellation-aware supervisor waits, durable generation-bound stop acknowledgement, pause-guard native failure diagnostics, startup legacy/duplicate inspection and repair, single-instance mutex handle release coverage, activity/window-enumeration ctypes signature coverage, timezone/DST analytics boundary coverage, legacy task migration fixture/normalization coverage, integrity-checked migration backup/corruption rejection, allowlisted export manifest/sensitive opt-in coverage, tray/App export owner-thread dispatch coverage, metadata-only inventory/clear controls, packaged retention dispatch coverage, diagnostic preview/sanitization coverage, frozen App path-snapshot coverage, and idempotent prompt/engine shutdown ownership coverage.

The bounded runner at `tools/verification_runner.py` currently reports passing stages for compileall, 164 unittest cases, QA scenario, app self-test, tray self-test, isolated native overlay self-test, settings inventory, diagnostic bundle generation, performance soak, profile isolation, repository-write guarding, and FocusCheck process-leak guarding. The unittest stage also exercises frozen source/frozen entrypoint resolution, frozen startup command generation, disposable install/upgrade/uninstall and package promotion/rollback, native overlay handle cleanup, busy-file-safe logging, runtime transition metadata, cross-process settings locking, PID-reuse-resistant supervisor lock ownership, sequence receipt-time heartbeat freshness and explicit cadence, atomic heartbeat publication and throttled write-failure diagnostics, stable-ready restart backoff reset, structured settings-save durability results, mainloop exception preservation through cleanup, manual-pause-preserving snooze transitions, tray task-dialog owner-thread dispatch, tray exit lifecycle without tray-thread process exit, explicit Windows idle API signatures and wrap-safe tick arithmetic, atomic legacy task/log migration with hash conflict preservation, UTC task timestamp normalization, settings input budgets, legacy settings import/conflict handling, diagnostic response redaction, boolean coercion for every boolean default in the schema, coordinator-owned website pause suppression, canonical website-domain matching, durable `allow_once` consumption, injected cooldown-clock boundaries, activity-confidence policy, generation-bound stale/foreign stop-request rejection, atomic stop-request writes, and the bounded packaged supervisor readiness/reaping harness contract. These stages do not prove the plan's native Windows, real installer shell, signing, browser provider, sleep/resume, registry, or full manual UI requirements.

The older stage-description sentence above retains the 164-case historical inventory; the current runner result is 200 cases as recorded in the latest rerun immediately above. The runner now contains 13 passing bounded stages, including `data_export`.

## Final Acceptance Gates

| Gate | Status | Reason |
| --- | --- | --- |
| A Repository/build | Partial | The source tree and pinned dependency contract are verified, a dual-executable PyInstaller build/self-test and install lifecycle contract are evidenced; real installer, signing, long-running supervisor, and deployment verification remain open. |
| B Automated verification | Partial | All thirteen bounded stages pass; integration breadth, thread/window leak checks, and full test-category coverage are absent. |
| C Settings/data | Partial | Canonical path selection, legacy settings import/conflict handling, migration, recovery, backup, atomic save, and revision conflict are tested; complete one-root migration for all data classes and all bounded-input fixtures are absent. |
| D Runtime state | Unverified | No complete truth-table/manual evidence proves pause, snooze, guards, effective pause, and duplicate-prompt invariants. |
| E Supervisor/startup | Partial | Supervisor regressions now cover a generation-bound stop protocol with stale/foreign request rejection and circuit-breaker behavior; live crash/hang/sleep and native startup evidence remain absent. |
| F Tk/tray/UI | Unverified | Source tests and self-tests exist, but owner-thread, tray uniqueness, deterministic dialog cleanup, and persistence-error UI evidence are incomplete. |
| G Prompts/interventions | Partial | Selected cancellation/cooldown behavior is tested; full V1/V2 matrices and orphan-overlay evidence are absent. |
| H Windows native | Unverified | No complete ctypes/resource audit or live lock/sleep/resume/multi-monitor/DPI evidence. |
| I Tasks/logs/privacy | Partial | DB migration safety, logs, allowlisted export, confirmed user-facing clear controls, metadata inventory, packaged retention, and diagnostic preview/sanitization hardening pass; export privacy evidence, complete privacy controls, and versioned recovery remain incomplete. |
| J Release evidence | Not met | Manual checklist is `not_run`; disposable upgrade/rollback/install lifecycle is tested, but target-machine installer/signing and release-package consistency evidence are not complete. |

## Explicit Scope Boundary

The plan itself says that static inspection must not be treated as proof of application launch, startup registry behavior, lock/sleep/resume, real browser detection, overlays, packaging, or user-data migration. Those items were checked for evidence and remain unverified or missing; they are not silently inferred from source presence or green automated tests.

## Verdict

The repository is an automated hardening checkpoint, not completion of the V1 refurbishment plan. Release status remains `NOT_READY`. The minimum evidence needed to change that verdict is the missing implementation work above plus completed manual Windows evidence and a reproducible packaging/rollback path.
