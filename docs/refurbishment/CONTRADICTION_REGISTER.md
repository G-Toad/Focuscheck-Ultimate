# Contradiction Register

| ID | Contradiction | Decision | Evidence | Status |
| --- | --- | --- | --- | --- |
| CONTR-001 | Normal supervised startup forced `FOCUSCHECK_FORCE_STARTED=1`, overriding persisted manual pause. | Normal supervision preserves pause; force-start is explicit only through `--force-start`. | `resolve_initial_monitoring_state`, launcher contract, QA, and supervisor forwarding tests. | fixed |
| CONTR-002 | `main.py` and `App` wrote different heartbeat files. | `App` owns one JSON `hb.txt` protocol consumed by the supervisor. | `paths.py`, `app.py`, supervisor heartbeat parser. | fixed |
| CONTR-003 | Existing finish docs described automated completion while the V1 plan requires native/manual evidence. | Automated, simulated, and manual evidence are separate labels. | refurbishment state and manual evidence files. | documented |
| CONTR-004 | The bounded verifier can pass while Tk/native/startup/browser/packaging behavior still lacks target-machine evidence. | Verification report `result` is `partial` whenever manual requirements remain; automated pass is not release readiness. | `tools/verification_runner.py`, `docs/refurbishment/verification-report.json`, `manual-evidence.json`. | fixed |
| CONTR-005 | Settings windows can be constructed standalone for compatibility while App-composed windows must not bypass the composition-root persistence boundary. | Standalone construction may retain its fallback; every composed Advanced, camera, crop, V1 prompt-footer, and V2 prompt-footer path receives the App persistence callback. | Settings UI and prompt regression tests; `CURRENT_STATE.md`. | fixed |
| CONTR-006 | The plan requires test categories that include both automated simulations and manual/destructive checks, but a single undifferentiated test count obscures that boundary. | A checked-in category manifest reports automated categories separately from live-Windows, manual-only, and destructive opt-in categories. | `docs/refurbishment/test-category-manifest.json`, `tools/test_category_inventory.py`, verification report. | fixed |
