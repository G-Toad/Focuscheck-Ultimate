# Archive

This folder preserves non-active material from the extracted `3-20260720T093815Z-1-001.zip` package.

- `scratch/` contains temporary root files and one-off patch/log artifacts.
- `legacy/` contains old archived code, exploratory scripts, and the original `nonsense/` and misspelled `archvie/` trees.
- `analysis/` contains generated multi-instance/Codex analysis output.
- `reference/` contains large reference dumps.
- `packages/` contains copied zip packages.
- `generated/` contains generated caches.
- `agent-config/` contains local assistant/tool configuration snapshots.

Nothing here is part of the active runtime path.
